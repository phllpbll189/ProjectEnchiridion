import React from 'react';
import '@testing-library/jest-dom'
import {render, fireEvent, waitFor, screen} from '@testing-library/react'
import EditBox from '../EditBox'

beforeEach(() => {
    render(
        <EditBox></EditBox>
    )
})

test("Can edit dropped components", () => {{

    }}
)

test("Can move dropped elements to a different position in the row.", () => {{

    }}
)

test("Can't add into full rows", () => {{

    }}
)
